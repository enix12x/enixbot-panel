{
  "name": "enixbot-panel",
  "version": "3.0.0",
  "description": "Admin panel for enixBot",
  "scripts": {
    "build": "tsc && cp src/index.html dist/ && cp src/app.js dist/",
    "serve": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "typescript": "^5.2.2"
  }
}

